pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh "echo Hello"
                sh "pwd"
                sh "hostaname"
            }
        }
    }
}